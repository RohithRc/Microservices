spring:
  application:
    name: "configserver"
  profiles:
#    active: native
    active: git
  cloud:
    config:
      server:
#         native:
#          search-locations: "classpath:/config"
          git:
            uri: "https://github.com/eazybytes/eazybytes-config"
            default-label: main
            timeout: 5
            clone-on-start: true
            force-pull: true

encrypt:
  key: "myS3cur3Encryptk3y@2026"

server:
  port: 8071
